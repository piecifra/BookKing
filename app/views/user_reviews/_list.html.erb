<% if @reviews.empty? %>
  <p>Nessuna recensione</p>
<% else %>
  <table>
    <thead>
      <tr>
        <th>Data</th>
        <th>Voto</th>
        <th>Utente</th>
        <th>Commento</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @reviews.each do |r| %>
        <tr>
          <td><%= r.created_at %></td>
          <td><%= r.rating %></td>
          <td><%= r.reviewer %></td>
          <td><%= r.comment %></td>
          <td>
          <% if can? :delete, r %>
            <%= link_to 'Elimina', destroy_user_review_path(r) %>
          <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<%end%>