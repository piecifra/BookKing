<%= form_with(model: desidered_book, local: true) do |form| %>
  <% if desidered_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(desidered_book.errors.count, "error") %> prohibited this desidered_book from being saved:</h2>

      <ul>
      <% desidered_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :desidered_book_nome %>
  </div>

  <div class="field">
    <%= form.label :autore %>
    <%= form.text_field :autore, id: :desidered_book_autore %>
  </div>

  <div class="field">
    <%= form.label :genere %>
    <%= form.text_field :genere, id: :desidered_book_genere %>
  </div>

  <div class="field">
    <%= form.label :anno %>
    <%= form.number_field :anno, id: :desidered_book_anno %>
  </div>

  <div class="field">
    <%= form.label :ISBN %>
    <%= form.number_field :ISBN, id: :desidered_book_ISBN %>
  </div>

  <%= form.hidden_field :user_id, :value => current_user.id %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
