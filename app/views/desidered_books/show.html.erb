<p id="notice"><%= notice %></p>

<div>
  <table class= "table table-striped table-bordered">
    <thead>
      <tr>
        <th>Nome</th>
        <th>Autore</th>
        <th>Genere</th>
        <th>Anno</th>
        <th>Isbn</th>
        <th>User</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
        <tr>
          <td><%= @desidered_book.nome %></td>
          <td><%= @desidered_book.autore %></td>
          <td><%= @desidered_book.genere %></td>
          <td><%= @desidered_book.anno %></td>
          <td><%= @desidered_book.ISBN %></td>
          <td><%= @desidered_book.user.username %></td>
          <% if can? :read, desidered_book %>
          <td><%= link_to 'Show', desidered_book %></td>
          <% end %>
          <% if can? :update, desidered_book %>
          <td><%= link_to 'Edit', edit_desidered_book_path(desidered_book) %></td>
          <% end %>
          <% if can? :delete, desidered_book %>
          <td><%= link_to 'Destroy', desidered_book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<p>
  <strong>Nome:</strong>
  <%= @desidered_book.nome %>
</p>

<p>
  <strong>Autore:</strong>
  <%= @desidered_book.autore %>
</p>

<p>
  <strong>Genere:</strong>
  <%= @desidered_book.genere %>
</p>

<p>
  <strong>Anno:</strong>
  <%= @desidered_book.anno %>
</p>

<p>
  <strong>Isbn:</strong>
  <%= @desidered_book.ISBN %>
</p>

<p>
  <strong>User:</strong>
  <%= @desidered_book.user.username %>
</p>

<%  if can? :update, @desidered_book%>
<%= link_to 'Modifica', desidered_book_path(@desidered_book) %> 
<% end %>
<%  if can? :destroy, @desidered_book%>
<%= link_to 'Elimina', @desidered_book, method: :delete, data: { confirm: 'Sei sicuro' } %>
<% end %>
<%= link_to 'Indietro', desidered_book_path %>
